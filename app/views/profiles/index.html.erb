<div class="col-sm-12 col-md-12 well">
<% @users = User.all %>
<% @users.each do |user| %>

  <%= image_tag user.avatar(:square),  :class => "img-polaroid" %>

  <%= link_to user.email, profile_path(user) %> <br/>

<% end %>
</div>